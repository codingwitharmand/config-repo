server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE}
    fetch-registry: true
    register-with-eureka: true
  instance:
  prefer-ip-address: true

inventory:
  initial-stock: 100

management:
  endpoints:
    web:
      exposure:
        include: "*"
